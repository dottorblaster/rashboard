// this is just a draft :F
var tile = {
	heading: "Draft of a tile",
	description: "This is a string",
	content: [
		"data here"
	],
	// so we can expand the widget or reduce it but
	// we have to know exactly where it breaks.
	// This way we can avoid resizing it too much.
	min-viewport: 4
};

console.log('loaded');
